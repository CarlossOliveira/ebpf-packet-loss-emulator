name: CI

on:
  push:
    branches: ["main"]
  pull_request:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install project dependencies
        run: make install

      - name: Install tooling
        run: | 
          sudo apt-get update
          sudo apt-get install -y build-essential clang-tools bear

      - name: Build project and generate compilation database
        run: bear --output compile_commands.json -- make clean all

      - name: Copy compilation database to build directory
        run: mv compile_commands.json build/src/

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ebpf-packet-loss-emulator
          path: |
            build/src/
          retention-days: 1


  static-analysis-clang-tidy:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - uses: actions/checkout@v4

      - name: Install project dependencies
        run: make install

      - name: Install tooling
        run: |
          sudo apt-get update
          sudo apt-get install -y clang-tidy clang-tools

      - name: Open compilation database
        uses: actions/download-artifact@v4
        with:
          name: ebpf-packet-loss-emulator
          path: build/src

      - name: Run clang-tidy
        run: |
          find src -name "*.c" ! -name "*.bpf.c" -print0 | \
          xargs -0 clang-tidy -p build/src/ \
            --extra-arg=-std=gnu17 \
            -header-filter=src/ \
            -checks=clang-analyzer-core.*,clang-analyzer-unix.*,bugprone-narrowing-conversions


  static-analysis-cppcheck:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - uses: actions/checkout@v4

      - name: Install project dependencies
        run: make install

      - name: Install tooling
        run: |
          sudo apt-get update
          sudo apt-get install -y cppcheck

      - name: Open compilation database
        uses: actions/download-artifact@v4
        with:
          name: ebpf-packet-loss-emulator
          path: build/src

      - name: Run cppcheck
        run: |
          cppcheck \
            --project=build/src/compile_commands.json \
            --std=c17 \
            --enable=all \
            --error-exitcode=1 \
            --suppress=unknownMacro \
            --suppress=missingIncludeSystem \
            --suppress=syntaxError:src/user-space/core/setup.c \
            --suppress=syntaxError:src/user-space/core/bpf_manager.c \
            --suppress=syntaxError:src/user-space/core/configuration.c \
            --suppress=unusedFunction


  code-format-clang-format:
    runs-on: ubuntu-latest
    needs:
      - static-analysis-clang-tidy
      - static-analysis-cppcheck

    steps:
      - uses: actions/checkout@v4

      - name: Install tooling
        run: |
          sudo apt-get update
          sudo apt-get install -y clang-format

      - name: Check formatting
        run: |
          clang-format --dry-run --Werror \
          $(find src -type f \( -name "*.c" -o -name "*.h" \) ! -name "vmlinux.h")


  test:
    runs-on: ubuntu-latest
    needs:
      - static-analysis-clang-tidy
      - static-analysis-cppcheck

    steps:
      - uses: actions/checkout@v4

      - name: Install project dependencies
        run: make install

      - name: Install tooling
        run: |
          sudo apt-get update
          sudo apt-get install -y jq

      - name: Open compilation database
        uses: actions/download-artifact@v4
        with:
          name: ebpf-packet-loss-emulator
          path: build/src
      
      - name: Give execute permissions to the binary
        run: sudo chmod +x build/src/bin/ebpf-packet-loss-emulator

      - name: Debug network state
        run: |
          ip link show

          echo "=== QDISCS ==="
          tc qdisc show

          echo "=== FILTERS INGRESS ==="
          tc filter show dev eth0 ingress || true

          echo "=== FILTERS EGRESS ==="
          tc filter show dev eth0 egress || true

          echo "=== BPF NET ==="
          bpftool net || true

          echo "=== BPF PROGRAMS ==="
          bpftool prog show || true

          echo "=== BPF LINKS ==="
          bpftool link show || true

      - name: Test module loading
        run: |
          sudo ./build/src/bin/ebpf-packet-loss-emulator \
          --run "list; load --module tc_bernoulli --interface eth0 --attachment-point ingress; exec sleep 15; unload; load -m xdp_bernoulli -i eth0 -a generic; exec sleep 15; exit;" \
          --dump ./logs/dump.txt

      - name: See test results
        run: cat ./logs/dump.txt

      - name: Confirm module loading
        run: |
          if ! grep -q "Module 'tc_bernoulli' loaded successfully" ./logs/dump.txt; then
            echo "error: tc_bernoulli module failed to load"
            exit 1
          fi
          if ! grep -q "Module 'xdp_bernoulli' loaded successfully" ./logs/dump.txt; then
            echo "error: xdp_bernoulli module failed to load"
            exit 1
          fi
